@model IEnumerable<DAPM_LT.Models.Kiemsoat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAd.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Imgtrangthai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trangthaisach)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sach.Tieude)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Imgtrangthai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trangthaisach)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sach.Tieude)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Idkiemsoat }) |
            @Html.ActionLink("Details", "Details", new { id=item.Idkiemsoat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Idkiemsoat })
        </td>
    </tr>
}

</table>
