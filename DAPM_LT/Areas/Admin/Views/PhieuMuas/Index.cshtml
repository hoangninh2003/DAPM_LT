@model IEnumerable<DAPM_LT.Models.PhieuMua>

@{ ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAd.cshtml"; }

<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">

<body>
    <div id="wrapper">
        <div id="page-wrapper">
            <div class="header">
                <h1 class="page-header">
                    Quản lí phiếu mua
                </h1>
                <div class="button-container">
                    <a class="btn btn-success" style="margin-right:auto" @Html.ActionLink("Create New", "Create")
                    </a>
                </div>
            </div>
            <div id="page-inner" style="margin-top:20px">
                <div class="row">
                    <div class="col-md-12">
                        <div class="card">
                            <div class="card-action">
                            </div>
                            <div class="card-content">
                                <div class="table-responsive">
                                    <div class="form-outline mb-4">
                                        <input type="search" placeholder="Search" class="form-control" id="datatable-search-input">
                                    </div>
                                    <div id="datatable">
                                    </div>
                                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                        <thead>
                                            <tr>
                                                <th>Mã Phiếu Mua</th>
                                                <th>Họ và Tên</th>
                                                <th>Ngày Mua</th>

                                            </tr>
                                        </thead>
                                        @foreach (var item in Model)
                                        {
                                            <tbody>
                                                <tr class="table-row" data-id="@item.Idphieu">
                                                    <td>@Html.DisplayFor(modelItem => item.Idphieu)</td>

                                                    <td>@Html.DisplayFor(modelItem => item.TaiKhoan.Holot) @Html.DisplayFor(modelItem => item.TaiKhoan.Ten)</td>

                                                    <td>@Html.DisplayFor(modelItem => item.Ngaymua)</td>

                                                </tr>

                                            </tbody>
                                        }
                                    </table>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        // Chọn tất cả các hàng có class "table-row"
        var rows = document.querySelectorAll('.table-row');

        // Thêm sự kiện click cho mỗi hàng
        rows.forEach(function (row) {
            row.addEventListener('click', function () {
                // Lấy giá trị id từ thuộc tính data-id
                var id = row.getAttribute('data-id');

                // Xây dựng URL chuyển hướng
                var redirectUrl = '@Url.Action("Index", "PhieuMuaChiTiets/Index")' + '/' + id;

                // Gửi yêu cầu chuyển hướng
                window.location.href = redirectUrl;
            });
        });
    });
</script>
</html>

@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Idsach)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Ngaymua)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TaiKhoan.Holot)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
    <tr style="text-align:center">
        <td>
            @Html.DisplayFor(modelItem => item.Idsach)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Ngaymua)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaiKhoan.Holot)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Idphieu }) |
            @Html.ActionLink("Details", "Details", new { id = item.Idphieu }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Idphieu })
        </td>
    </tr>
    }

    </table>*@
