@model IEnumerable<DAPM_LT.Models.PhanQuyen>

@{
    ViewBag.Title = "Danh Sách Quyền";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAd.cshtml";
}

<div class="container">
    <h2>Danh Sách Quyền</h2>

    <div class="button-container">
        @Html.ActionLink("Thêm Quyền", "Create", null, new { @class = "btn btn-primary" })
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Tên Quyền</th>
                <th>Thao Tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.TenQuyen)</td>
                    <td>
                        @Html.ActionLink("Sửa", "Edit", new { id = item.IDQuyen }, new { @class = "btn btn-secondary btn-sm" })
                        @Html.ActionLink("Chi Tiết", "Details", new { id = item.IDQuyen }, new { @class = "btn btn-info btn-sm" })
                        @Html.ActionLink("Xóa", "Delete", new { id = item.IDQuyen }, new { @class = "btn btn-danger btn-sm" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>




<style>
    .container {
        margin: 20px;
    }

    .button-container {
        margin-bottom: 20px;
    }

    .table th, .table td {
        text-align: center;
    }

    .btn-sm {
        padding: 5px 10px;
        font-size: 14px;
    }

    .btn-secondary, .btn-info, .btn-danger {
        margin-right: 5px;
    }

</style>